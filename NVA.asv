function [V_n] = NVA(par, CB, BT)
%NVA Nodal voltage analysis Script 3
%   Detailed explanation goes here
Z_l = load_impedance(par, CB, BT);
[V_t Z_t] = thevenin_eq(par, CB, BT);
if BT == 1
    %Bus is closed
    %Port
    V_n(1) = (eye(3)*(1/Z_l(1) + 1/Z_t(1)))\V_t(:, 1);
    %Starb
end

